# Route all requests through index.php
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?_route=$1 [QSA,L]

# Block direct HTTP access to the config file
<Files "mailframe.config.json">
    Order deny,allow
    Deny from all
</Files>
